# Public — surfaced to the browser bundle. Never put secrets here.
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_WS_URL=ws://localhost:8083/ws

# Tauri desktop builds (`pnpm tauri build`)
SEQUOIA_DESKTOP=0
